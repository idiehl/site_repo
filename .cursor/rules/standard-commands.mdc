---
description: Standard commands used across Atlas Universalis tasks
alwaysApply: true
---

# Standard Commands (use as needed)

- `git status -sb`
- `git diff`
- `git log -5 --oneline`

- `ssh root@167.71.179.90 "cd /var/www/atlasuniversalis.com && git pull origin master"`

- `.\mcp\.venv\Scripts\python.exe -m pip install -r mcp\requirements.txt`
